{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "schema": "storyline.v3",
  "title": "Storyline worker submission",
  "type": "object",
  "additionalProperties": true,
  "required": ["core_answer", "sections", "visual_evidence_plan"],
  "properties": {
    "core_answer": {"type": "string", "minLength": 1},
    "sections": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["chapter", "heading", "brief", "finding_refs"],
        "properties": {
          "chapter": {
            "type": "string",
            "minLength": 1,
            "description": "Concise chapter/section name used to identify the topic, not the conclusion."
          },
          "heading": {
            "type": "string",
            "minLength": 1,
            "description": "The section leadline: a conclusion-led title that states the judgement established by this section."
          },
          "brief": {
            "type": "string",
            "minLength": 1,
            "description": "The core argument explaining how the section establishes its leadline."
          },
          "finding_refs": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          },
          "visual_evidence_refs": {
            "type": "array",
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          }
        }
      }
    },
    "visual_evidence_plan": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "claim", "purpose", "evidence_refs", "data_type", "required", "placement", "section_heading", "order"],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "claim": {"type": "string", "minLength": 1},
          "purpose": {"type": "string", "minLength": 1},
          "evidence_refs": {"type": "array", "minItems": 1, "items": {"type": "string", "minLength": 1}, "uniqueItems": true},
          "data_asset_refs": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true},
          "data_type": {"type": "string", "enum": ["time_series", "comparison", "distribution", "relationship", "process", "qualitative"]},
          "required": {"type": "boolean"},
          "placement": {"type": "string", "enum": ["opening", "section"]},
          "section_heading": {"type": "string", "minLength": 1},
          "order": {"type": "integer", "minimum": 1}
        }
      }
    },
    "appendix_finding_refs": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "open_issues": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    }
  }
}
